{
  "name": "DT-CallCenter-app",
  "version": "1.0.0",
  "description": "DT Callcenter app",
  "main": "",
  "scripts": {
    "prepare": "mkdir -p .well-known && ln -sf ../node_modules/runtime-browser/bin/ .well-known/runtime",
    "start": "npm run prepare && live-server --no-browser",
    "init-setup": "npm install -g live-server && npm install"
  },
  "author": "reThink Project",
  "contributors": [
    "Steffen Drüsedow <steffen.druesedow@telekom.de>"
  ],
  "license": "ISC",
  "devDependencies": {
  },
  "dependencies": {
    "runtime-browser": "github:reTHINK-project/dev-runtime-browser#v0.5.0"
  }
}
